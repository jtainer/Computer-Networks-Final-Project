#ifndef CONFIG_H
#define CONFIG_H

// If any constant is needed in more than one source file, put it here

constexpr int tileMapWidth = 32;
constexpr int tileMapHeight = 32;
constexpr int tileMapCount = tileMapWidth * tileMapHeight;

#endif